# Fly.io deployment. `fly launch --no-deploy` will offer to overwrite this —
# decline, because the volume mount and the single-machine constraint below are
# both load-bearing for a SQLite app.
app = "writing-assistant"
# Mumbai. With one machine there is no edge to fall back on, so the region IS
# the latency for every user — and an autosaving editor feels the difference.
# Virginia would add roughly 200ms round trip for Indian users.
# Confirm the code is still current with: fly platform regions
primary_region = "bom"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "3000"
  DATABASE_URL = "/data/app.db"

# SQLite is a file on disk, so the machine needs one that survives deploys.
[mounts]
  source = "writing_data"
  destination = "/data"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  # One machine, always. A second machine would get its own volume and its own
  # divergent copy of the database — the classic way to lose half your users'
  # writing without any error appearing anywhere.
  min_machines_running = 1

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/api/health"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
